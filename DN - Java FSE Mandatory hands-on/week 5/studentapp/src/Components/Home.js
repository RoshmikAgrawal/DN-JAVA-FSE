import React, { Component } from 'react';

/**
 * Class component representing the landing portal workspace view.
 * Uses explicit export naming to bind perfectly with the application router context.
 */
export class Home extends Component {
    render() {
        return (
            <div>
                <h3>Welcome to the Home Page of Student Management Portal</h3>
            </div>
        );
    }
}