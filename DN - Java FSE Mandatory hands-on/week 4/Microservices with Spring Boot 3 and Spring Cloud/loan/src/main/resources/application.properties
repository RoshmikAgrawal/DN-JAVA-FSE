# Microservice Server Port Configuration
# Overriding the default port framework configuration to 8081 to completely prevent
# TCP socket bind address collision states during concurrent microservice instantiation routines
server.port=8081

# Application Identity Mapping Registry
# Establishes the logical lookup identification name within the centralized discovery architecture
spring.application.name=loan-service